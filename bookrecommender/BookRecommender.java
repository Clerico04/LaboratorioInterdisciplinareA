/**
 * BookRecommender.java
 * 
 * Questa classe rappresenta il punto di ingresso principale dell'applicazione 
 * Book Recommender. L'applicazione offre una GUI per visualizzare e gestire
 * raccomandazioni e valutazioni di libri.
 * 
 * @author LucaClerici756176CO
 * @author AlessandroMonaci757003CO
 * @version 1.0
 */

package bookrecommender;
import java.io.*;

/**
 * La classe BookRecommender è il punto di avvio dell'applicazione.
 * Inizializza l'interfaccia utente grafica (GUI) per l'applicazione.
 */

public class BookRecommender{

    /**
     * Il metodo main che avvia l'applicazione.
     * 
     * @param args Argomenti della riga di comando (non utilizzati).
     */
    public static void main(String[] args){
        GUI gui = new GUI();
    }
}